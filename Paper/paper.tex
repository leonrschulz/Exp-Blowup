\documentclass[a4paper,USenglish,cleveref, autoref, thm-restate]{lipics-v2021}
%This is a template for producing LIPIcs articles.
%See lipics-v2021-authors-guidelines.pdf for further information.
%for A4 paper format use option "a4paper", for US-letter use option "letterpaper"
%for british hyphenation rules use option "UKenglish", for american hyphenation rules use option "USenglish"
%for section-numbered lemmas etc., use "numberwithinsect"
%for enabling cleveref support, use "cleveref"
%for enabling autoref support, use "autoref"
%for anonymousing the authors (e.g. for double-blind review), add "anonymous"
%for enabling thm-restate support, use "thm-restate"
%for enabling a two-column layout for the author/affilation part (only applicable for > 6 authors), use "authorcolumns"
%for producing a PDF according the PDF/A standard, add "pdfa"

%\pdfoutput=1 %uncomment to ensure pdflatex processing (mandatatory e.g. to submit to arXiv)
%\hideLIPIcs  %uncomment to remove references to LIPIcs series (logo, DOI, ...), e.g. when preparing a pre-final version to be uploaded to arXiv or another public repository

%\graphicspath{{./graphics/}}%helpful if your graphic files are in another directory

\usepackage{amssymb}
\newcommand{\Nat}{\mathbb{N}}

\usepackage{cleveref}

\theoremstyle{definition}
\newtheorem{definitionx}[theorem]{Definition}

\crefname{definitionx}{Definition}{Definitions}
\Crefname{definitionx}{Definition}{Definitions}

\bibliographystyle{plainurl}% the mandatory bibstyle

\hideLIPIcs

\title{Formalizing the Exponential Blowup in the Transformations between CNF and DNF}

%\titlerunning{} %TODO optional, please use if title is longer than one line

\def\lmu{Ludwig-Maximilians-Universität München}

\author{Leon Raffael Schulz}{\lmu{}, Germany}{Le.Schulz@campus.lmu.de}{https://orcid.org/0009-0007-7666-2034}{}
%TODO (MDS): Fix the ERC macros.
\author{Martin Desharnais-Schäfer}{\lmu{}, Germany}{desharnais-schaefer@ifi.lmu.de}{https://orcid.org/0000-0002-1830-7532}{
This research was co-funded by the European Union (ERC, Nekoka, 101083038). Views and opinions expressed are however those of the authors only and do not necessarily reflect those of the European Union or the European Research Council. Neither the European Union nor the granting authority can be held responsible for them.
}
\author{Jan Johannsen}{\lmu{}, Germany}{jan.johannsen@ifi.lmu.de}{https://orcid.org/0009-0004-2072-4679}{}

\authorrunning{Leon Raffael Schulz, Martin Desharnais-Schäfer, and Jan Johannsen}

\Copyright{Leon Raffael Schulz and Martin Desharnais-Schäfer and Jan Johannsen}

\ccsdesc[500]{Theory of computation~Logic and verification} %TODO mandatory: Please choose ACM 2012 classifications from https://dl.acm.org/ccs/ccs_flat.cfm

\keywords{Propositional logic, disjunctive normal form, conjunctive normal form, formula size, Isabelle/HOL} %TODO mandatory; please add comma-separated list of keywords

\category{} %optional, e.g. invited paper

\relatedversion{} %optional, e.g. full version hosted on arXiv, HAL, or other respository/website
%\relatedversiondetails[linktext={opt. text shown instead of the URL}, cite=DBLP:books/mk/GrayR93]{Classification (e.g. Full Version, Extended Version, Previous Version}{URL to related version} %linktext and cite are optional

%\supplement{}%optional, e.g. related research data, source code, ... hosted on a repository like zenodo, figshare, GitHub, ...
%\supplementdetails[linktext={opt. text shown instead of the URL}, cite=DBLP:books/mk/GrayR93, subcategory={Description, Subcategory}, swhid={Software Heritage Identifier}]{General Classification (e.g. Software, Dataset, Model, ...)}{URL to related version} %linktext, cite, and subcategory are optional

%\funding{(Optional) general funding statement \dots}%optional, to capture a funding statement, which applies to all authors. Please enter author specific funding statements as fifth argument of the \author macro.

\acknowledgements{
This paper is based on Schulz's bachelor thesis \cite{Schulz_bsc_thesis} supervised by Johannsen with the help of Desharnais-Schäfer.
%TODO (LRS): try to upload bsc thesis to https://epub.ub.uni-muenchen.de/help/studienabschlussarbeiten.html
}%optional

%\nolinenumbers %uncomment to disable line numbering



%Editor-only macros:: begin (do not touch as author)%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\EventEditors{Jasmin Blanchette, Dmitriy Traytel, and Makarius Wenzel}
\EventNoEds{3}
\EventLongTitle{Isabelle Workshop 2026}
\EventShortTitle{}
\EventAcronym{}
\EventYear{2026}
\EventDate{24--25 July 2026}
\EventLocation{Lisbon, Portugal}
\EventLogo{}
\SeriesVolume{}
\ArticleNo{}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% TODO: write a Makefile that uses podman to compile the paper in a container based on TL2025-historic


\input{def.tex}

%TODO: Change label of proof environment to "Proof sketch"

\begin{document}

\maketitle

\begin{abstract}
  A well-known folklore result about propositional logic is that transforming a formula in disjunctive or conjunctive normal form can lead to an exponential blowup of the formula size.
  We formalize this result in the form of two theorems in Isabelle/HOL and discuss the challenges we encountered.
\end{abstract}

\section{Introduction}
\label{sec_intro}

The metatheory of propositional logic has been studied for many decades and has been the subject of extensive formalization effort for many years.
However, to the best of our knowledge, very few quantitative results have been formalized.
One well-known quantitative folklore result is that transforming a formula in disjunctive or conjunctive normal form can lead to an exponential blowup of the formula size.

In this paper, we first briefly cover the necessary mathematical background (\cref{sec_background}) and then report on our Isabelle/HOL formalization of this exponential blowup.
We start by presenting a few preliminary definitions and lemmas (\cref{sec_preliminaries}).

We then prove the exponential blowup when converting a formula from conjunctive to disjunctive normal form (\cref{sec_CNF_to_DNF}).
We do this in two steps:
First, we prove the low-level \cref{thm_cnf_to_dnf_blowup} that assumes the existence of two equivalent formulas fulfilling some assumptions and that exhibits the exponential lower bound on the formula size.
Second, we use this \lcnamecref{thm_cnf_to_dnf_blowup} to prove in the desired \cref{thm_cnf_to_dnf_blowup_math} that such formulas always exist.

We analogously prove the same exponential blowup when converting a formula from disjunctive to conjunctive normal form (\cref{sec_DNF_to_CNF}).
We do this again in two steps:
First, the low-level \Cref{thm_dnf_to_cnf_blowup} assumes again the existence of two equivalent formulas fulfilling some assumptions and exhibits the exponential lower bound on the formula size;
the proof uses \cref{thm_cnf_to_dnf_blowup}.
Second, we use \cref{thm_dnf_to_cnf_blowup} to prove in the desired \cref{thm_dnf_to_cnf_blowup_math} that such formulas always exist.

Finally, we discuss (\cref{sec_discussion}) some of the challenges we encountered when proving statements about the size of formulas.

Our work is part of the IsaFoL (Isabelle Formalization of Logic) effort \cite{Blanchette_CPP2019_IsaFoL}.
Our formalization consists of approximately 1100 nonempty lines%
\footnote{Counted using \inlineshell{grep -Ev '\textasciicircum{}[[:blank:]]*\$'}.}
and is available online \cite{Formalization_GitHub};
we will soon submit it to the \emph{Archive of Formal Proofs}.
It was developed with Isabelle2025 \cite{Nipkow_Klein_2014,Nipkow_Paulson_Wenzel_2002} and with extensive usage of Sledgehammer \cite{Blanchette_Boehme_Paulson_JAR2013,Paulson_Blanchette_IWIL2010} and the structured Isar language \cite{Wenzel_2007}.
In this paper, we only provide some interesting lemmas and proof sketches;
we refer interested readers to the formalization for more details.

\section{Background}
\label{sec_background}

A \emph{formula} of propositional logic is defined inductively as either falsehood ($\bot$), an atom (or \emph{variable}), the negation ($\neg$) of a formula, or the disjunction ($\lor$), conjunction ($\land$) or implication ($\rightarrow$) of two formulas.
A \emph{literal} is either an atom $x$, a negated atom $\neg x$, falsehood $\bot$, or negated falsehood $\neg\bot$.
The literals $x$ and $\neg x$ as well as $\bot$ and $\neg\bot$ are \emph{complements} of each other.

A \emph{valuation} is a function from atoms to Boolean values.
The \emph{evaluation} of a formula w.r.t.\ a valuation is the Boolean value obtained after replacing all atoms with the specific truth values assigned by the valuation and evaluating according to the usual Boolean evaluation rules.

We say that a valuation $\alpha$ \emph{satisfies} a formula $\phi$, written $\alpha \models \phi$, if the formula evaluates to true w.r.t.\ $\alpha$.
A formula $\phi$ is
\emph{satisfiable} if there exists a valuation that satisfies it (i.e., $\existsOne{\alpha}{\alpha \models \phi}$) and
\emph{tautological} if it is satisfied by every valuation (i.e., $\forallOne{\alpha}{\alpha \models \phi}$).
Two formulas $\phi$ and $\psi$ are \emph{equivalent} if they evaluate to the same truth value for every valuation (i.e., $\forallOne{\alpha}{\alpha \models \phi \iff \alpha \models \psi}$).

A formula $\phi$ is in \emph{negation normal form} if $\phi$ contains no implications and if negations only occur immediately in front of atoms or falsehood
(i.e., $\phi$ is built from literals by conjunction and disjunction only).

For a formula $\phi$ in negation normal form we define the dual formula
$\bar{\phi}$ in negation normal form by replacing every literal by its
complement and interchanging the symbols $\land$ and $\lor$.
For every formula $\phi$ in negation normal form, $\bar{\phi}$ is equivalent to $\neg \phi$.

\begin{definitionx}
  We define the following classes of formulas:
\begin{itemize}
\item A \emph{term} is a conjunction $a_1 \land \ldots \land a_k$
  of literals.
\item A \emph{clause} is a disjunction $a_1 \lor \ldots \lor a_k$ of literals.
\item A formula in \emph{disjunctive normal form} (DNF) is a
  disjunction $T_1 \lor \ldots \lor T_m$ of terms.
\item A formula in \emph{conjunctive normal form} (CNF) is a
  conjunction $C_1 \land \ldots \land C_m$ of clauses.
\end{itemize}
\end{definitionx}

Note that every formula in disjunctive or conjunctive
normal form is also in negation normal form. For a formula $\phi$ in
conjunctive normal form, the dual formula $\bar \phi$ is in disjunctive
normal form, and vice versa.

It is well known that every formula of propositional logic can be transformed
into an equivalent formula in disjunctive normal form and
into an equivalent formula in conjunctive normal form.
However, the cost of these transformations is an
exponential increase in the formula size in the worst case (see, e.g., the textbook of
Kleine B\"uning and Lettmann \cite{KleineBueningLettmann_Aussagenlogik}).

\begin{theorem} \label{dnfblowup}
  For all sufficiently large $n\in\Nat$, there are formulas $F_n$ in
  conjunctive normal form with $|F_n| = O(n)$, such that every equivalent formula $G_n$ in
  disjunctive normal form is of size $|G_n| \geq n \cdot 2^n$.
\end{theorem}
\begin{proof}
The formulas $F_n$ are defined as
$F_n = \bigwedge_{i=1}^n ( x_{i,0} \lor x_{i,1} ) \land (\neg x_{i,0} \lor \neg x_{i,1})$.

If $G_n = \bigvee_{j\leq m} T_j$ is a formula in conjunctive normal form equivalent to $F_n$, then the following two claims hold, which together prove the statement.

Claim 1. Each of the terms $T_j$ contains exactly one of the literals $x_{i,0}$ and $x_{i,1}$, for every $i\leq n$;
otherwise, there is a valuation that satisfies this term, and thus $G_n$, but does not satisfy $F_n$.
It follows that every term $T_j$ is of size at least $n$.

Claim 2. For every vector
$\epsilon = (\epsilon_1 , \ldots ,\epsilon_n) \in \{ 0,1 \}^n$,
there is one of the terms $T_j$ in  $G_n$, which contains the literals
$x_{1,\epsilon_1}, \ldots , x_{n,\epsilon_n}$;
otherwise, we can find a valuation that satisfies $F_n$, but does not satisfy $G_n$.
It follows that $G_n$ has at least $m \geq 2^n$ many terms.
\end{proof}

\begin{theorem} \label{cnfblowup}
  For all sufficiently large $n\in\Nat$, there are formulas $F'_n$ in
  disjunctive normal form with $|F'_n| = O(n)$, such that every equivalent formula $G_n$
  in conjunctive normal form is of size $|G_n| \geq n \cdot 2^n$.
\end{theorem}

\begin{proof}
  Let $F'_n = {\bar F}_n$ for the formula $F_n$ from the proof of \cref{dnfblowup}.
  If there was a formula  $G_n$ in conjunctive normal form equivalent to $F'_n$ with $\left|G_n\right| < n \cdot 2^n$,
  then ${\bar{G}}_n$ would be a formula in disjunctive normal form equivalent to $F_n$ with
  $|{\bar{G}}_n| = |G_n| < n \cdot 2^n$, contradicting \cref{dnfblowup}.
\end{proof}

% TODO: Cite at least the book, and check for transitive citations.
% TODO: All definitions required to understand the proposition and corollary.
% TODO: With proof sketch so the formalization can later compare the result.
% TODO: Related work?

\section{Preliminaries}
\label{sec_preliminaries}

We reused the theories of proposition formulas from Michaelis and Nipkow's formalization of proof systems for classical propositional logic \cite{Propositional_Proof_Systems-AFP,Michaelis_Nipkow_TYPES2017}.
Formulas have type $\tyFormula{\yA}$, where $\yA$ is an abstract type of atoms.
Basic operations on formulas and related lemmas are expressed for any atom type.
However, our main theorems instantiate the atom type with variables $x_{i,b}$, where $i$ is a natural number and $b$ is a Boolean value.

A formula is \emph{conjunctive} if it is either a literal or its first subformula is a literal and its second a conjunctive formula.
Analogously a formula is \emph{disjunctive} if it is either a literal or its first subformula is a literal and its second a disjunctive formula.

We now define some operations to build the conjunction and disjunction of a list of formulas.

\begin{definitionx}
  \label{def_BigOr_BigAnd}
  The functions
  $\tyAnnot{\cst{BigAnd'}}{\tyList{\tyFormula{\yA}} \Rightarrow \tyFormula{\yA}}$ and
  $\tyAnnot{\cst{BigOr'}}{\tyList{\tyFormula{\yA}} \Rightarrow \tyFormula{\yA}}$ respectively
  build the conjunction and disjunction of all formulas in a list:%
  \footnote{The infix operation $\listCons{x}{xs}$ adds an element $x$ at the front of a list $xs$.}
  \begin{align*}
    \isaAp{\cst{BigAnd'}}{[]} =\; & \neg\bot &
    \isaAp{\cst{BigOr'}}{[]} =\; & \bot \\
    \isaAp{\cst{BigAnd'}}{[\phi]} =\; & \phi &
    \isaAp{\cst{BigOr'}}{[\phi]} =\; & \phi \\
    \isaAp{\cst{BigAnd'}}{(\listCons{\phi}{\phi{}s})} =\; & \phi \land \isaAp{\cst{BigAnd'}}{\phi{}s} &
    \isaAp{\cst{BigOr'}}{(\listCons{\phi}{\phi{}s})} =\; & \phi \lor \isaAp{\cst{BigOr'}}{\phi{}s}
  \end{align*}
  In the rest of this paper, we write
  $\bigwedge_{i = 0}^{\listLength{\phi{}s}} \listNth{\phi{}s}{i}$ for $\isaAp{\cst{BigAnd'}}{\phi{}s}$ and
  $\bigvee_{i = 0}^{\listLength{\phi{}s}} \listNth{\phi{}s}{i}$ for $\isaAp{\cst{BigOr'}}{\phi{}s}$.
\end{definitionx}

\begin{remark*}
  Our formalization always uses nonempty lists and the base cases of \cref{def_BigOr_BigAnd} could be be left unspecified.
  However, providing these base cases allows us to prove many lemmas for arbitrary lists without requiring a nonemptyness assumption that would need to be repeatedly discharged.
\end{remark*}

We will later need to generate lists of formulas for $\cst{BigAnd'}$ and $\cst{BigOr'}$ and define two functions to do so.

\begin{definitionx}
  \label{def_unconj_undisj}
  The functions
  $\tyAnnot{\cst{unconj}}{\tyFormula{\yA} \Rightarrow \tyList{\tyFormula{\yA}}}$ and
  $\tyAnnot{\cst{undisj}}{\tyFormula{\yA} \Rightarrow \tyList{\tyFormula{\yA}}}$ respectively
  extract the list of subformulas directly under the conjunctions and disjunctions of a given formula:%
  \footnote{The infix operation $xs \mathop{@} ys$ appends a list $xs$ at the front of a list $ys$.}
  \begin{align*}
    \isaAp{\cst{unconj}}{(\phi \land \psi)} =\; & \isaAp{\cst{unconj}}{\phi} \mathop{@} \isaAp{\cst{unconj}}{\psi} &
    \isaAp{\cst{undisj}}{(\phi \lor \psi)} =\; & \isaAp{\cst{undisj}}{\phi} \mathop{@} \isaAp{\cst{undisj}}{\psi} \\
    \isaAp{\cst{unconj}}{\phi} =\; & [\phi] &
    \isaAp{\cst{undisj}}{\phi} =\; & [\phi]
  \end{align*}
\end{definitionx}

We will also need to construct formulas of a very specific family and define a function to do so.

\begin{definitionx}
  \label{def_Fn}
  The function $\tyAnnot{\cst{Fn}}{\tyNat \Rightarrow \tyFormula{\yA}}$ creates a formula containing
  the conjunction of the exclusive disjunctions of each variable pair $x_{i,false}$ and $x_{i,true}$ with $i \in \{1,\ldots,n\}$:
  \begin{align*}
    \isaAp{\cst{Fn}}{0} =\; & \neg\bot &  \\
    \isaAp{\cst{Fn}}{(n+1)} =\; & ((x_{i,false} \lor x_{i,true}) \land (\neg x_{i,false} \lor \neg x_{i,true})) \land \isaAp{\cst{Fn}}{n}
  \end{align*}
  In the rest of this paper, we write $F_n$ for $\isaAp{\cst{Fn}}{n}$.
\end{definitionx}

\Cref{def_Fn} is chosen such that a formula $F_n$ is in conjunctive normal form and can only be satisfied by valuations that assign different truth values to the variables $x_{i,false}$ and $x_{i,true}$.

\begin{lemma}
  \label{thm_is_cnf_Fn}
  Let $n$ be a natural number.
  The formula $F_n$ is in conjunctive normal form.
\end{lemma}

\begin{lemma}
  \label{thm_semantics_Fn_iff}
  Let $n$ be a natural number.
  Let $\alpha$ be a valuation.
  The valuation $\alpha$ satisfies $F_n$ if and only if $(\isaAp{\alpha}{x_{i,false}}) \neq (\isaAp{\alpha}{x_{i,true}})$ for every $1 \le i \le n$.
\end{lemma}

Finally, we provide two different definitions for the size of a formula.

\begin{definitionx}
  \label{def_size}
  The function $\tyAnnot{\cstSize}{\tyFormula{\yA} \Rightarrow \tyNat}$ counts the number of atoms, $\bot$, negations, disjunctions, conjunctions, and implications in a formula.
  In the rest of this paper, we write $|\phi|$ for $\isaAp{\cstSize}{\phi}$.
\end{definitionx}

\begin{definitionx}
  \label{def_sizef}
  The function $\tyAnnot{\cstSizef}{\tyFormula{\yA} \Rightarrow \tyNat}$ counts the number of atoms, $\bot$, disjunctions, conjunctions, and implications in a formula;
  the negations are ignored.
\end{definitionx}

Because $\cst{sizef}$ ignores negations, it always evaluates to a smaller number than $\left|\cdot\right|$.

\begin{lemma}
  \label{thm_sizef_le_size}
  Let $\phi$ be a formula.
  $\isaAp{\cstSizef}{\phi} \le \left|\phi\right|$.
\end{lemma}

We can observe the difference and similarity between the two definitions by looking at the sizes of the $F_n$ formula and the conjunction and disjunction of lists of formulas.

\begin{lemma}
  \label{thm_sizef_Fn}
  Let $n$ be a natural number.
  $\isaAp{\cstSizef}{F_n} = 8 \cdot n + 1$ and
  $\left|F_n\right| = 10 \cdot n + 2$.
\end{lemma}

\begin{lemma}
  \label{thm_sizef_BigOr_undnf_BigAnd_unconj}
	Let $\phi$ be a formula in conjunctive normal form and $\psi$ be a formula in disjunctive normal form.
  The following equalities hold:
  \begin{align*}
    \isaAp{\cstSizef}{\left(\bigwedge\nolimits_{i = 0}\nolimits^{\listLength{\isaAp{\cst{unconj}}{\phi}}} \listNth{(\isaAp{\cst{unconj}}{\phi})}{i}\right)} =\:& \isaAp{\cstSizef}{\phi} &
    \left|\bigwedge\nolimits_{i = 0}\nolimits^{\listLength{\isaAp{\cst{unconj}}{\phi}}} \listNth{(\isaAp{\cst{unconj}}{\phi})}{i}\right| =\:& \left|\phi\right|
    \\
    \isaAp{\cstSizef}{\left(\bigvee\nolimits_{i = 0}\nolimits^{\listLength{\isaAp{\cst{undisj}}{\psi}}} \listNth{(\isaAp{\cst{undisj}}{\psi})}{i}\right)} =\:& \isaAp{\cstSizef}{\psi} &
    \left|\bigvee\nolimits_{i = 0}\nolimits^{\listLength{\isaAp{\cst{undisj}}{\psi}}} \listNth{(\isaAp{\cst{undisj}}{\psi})}{i}\right| =\:& \left|\psi\right|
  \end{align*}
\end{lemma}

Most of the statements about the size of formulas are expressed in terms of $\cstSizef$ but, luckily, \cref{thm_sizef_le_size} will allow us to remove $\cstSizef$ entirely from the statements of \cref{thm_cnf_to_dnf_blowup_math,thm_dnf_to_cnf_blowup_math}.


\section{CNF to DNF}
\label{sec_CNF_to_DNF}

Our formalization splits the paper proof of \cref{dnfblowup} in a low-level \lcnamecref{thm_cnf_to_dnf_blowup}, which assumes the existence of formulas with specific shapes and properties, and a \lcnamecref{thm_cnf_to_dnf_blowup_math}, which proves that such formulas can always be obtained.
We start by proving a few basic properties of conjunctive formulas w.r.t.\ valuations.

We first prove a monotonicity property of satisfiability w.r.t. valuations.

\begin{lemma}
  \label{aux_lemma_prop4_step1}
  Let $\phi$ be a conjunctive formula.
  Let $\alpha$ and $\beta$ be valuations.
  If both valuations map every variable occurring in $\phi$ to the same Boolean value and
  if $\alpha$ satisfies $\phi$,
  then $\beta$ also satisfies $\phi$.
\end{lemma}

Next, we prove some sufficient conditions for a valuation to not satisfy a conjunctive formula.

\begin{lemma}
  \label{aux_lemma_prop4_step3}
  Let $\phi$ be a conjunctive formula.
  Let $\alpha$ be a valuation.
  If
  \begin{itemize}
    \item there exists a variable that occurs positively in $\phi$ and is mapped to false by $\alpha$ or
    \item there exists a variable that occurs negatively in $\phi$ and is mapped to true by $\alpha$,
  \end{itemize}
  then $\alpha$ does not satisfy $\phi$.
\end{lemma}

Next, we prove a lower bound of the size of $\bigvee_{i=0}^{\listLength{\phi{}s}} \listNth{\phi{}s}{i}$.

\begin{lemma}
  \label{thm_exp_size}
  Let $n > 0$ be a natural number.
  Let $\phi{}s$ be a list of formulas.
  If every $\phi \in \isaAp{\cst{set}}{\phi{}s}$ has $\isaAp{\cstSizef}{\phi} \ge m$ and
  if $\listLength{\phi{}s} \ge 2^n$,
  then $\isaAp{\cstSizef}{(\bigvee_{i=0}^{\listLength{\phi{}s}} \listNth{\phi{}s}{i})} \ge m \cdot 2^n$.%
  \footnote{The function $\tyAnnot{\cst{set}}{\tyList{\yA} \Rightarrow \tySet{\yA}}$ converts a list into a set.}
\end{lemma}

We can now prove that some specific formulas equivalent to $F_n$ (see \cref{def_Fn}) have a size exponential in $n$.

\begin{proposition}
  \label{thm_cnf_to_dnf_blowup}
  Let $n > 0$ be a natural number.
  Let $Ts$ be a list of formulas.
  Let $G = \bigvee_{i = 0}^{\listLength{Ts}} \listNth{Ts}{i}$.
  If all formulas in $Ts$ are conjunctive and satisfiable, and
  if $F_n$ is equivalent to $G$,
  then $\isaAp{\cstSizef}{G} \ge n \cdot 2^n$.
\end{proposition}
\begin{proof}
  We proceed with five steps:
  \textbf{(1)}~First, we show that every conjunctive formula $T \in \isaAp{\cst{set}}{Ts}$ contains for all $i \in \{1,\ldots,n\}$ either the variable $x_{i,0}$ or $x_{i,1}$ positively.
  We prove this step by negation, where we have to derive false for two cases:
  Either the variables $x_{i,0}$ and $x_{i,1}$ are both positively contained in $T$ or they are both not positively contained.
  In both cases, we proceed by defining a valuation that satisfies $G$ but not $F_n$.
  For instance, in the second case, we find a valuation that satisfies $T$ and change the assignment of the variables $x_{i,0}$ and $x_{i,1}$ to false.
  We then show using \cref{aux_lemma_prop4_step1,aux_lemma_prop4_step3,thm_semantics_Fn_iff} that this modified valuation satisfies $T$ (and thus $G$) but does not satisfy $F_n$.
  This means that $F_n$ is not equivalent to $G$, which contradicts the \lcnamecref{thm_cnf_to_dnf_blowup}'s assumption.
  \textbf{(2)}~From step 1, we derive that every $T \in \isaAp{\cst{set}}{Ts}$ has $\isaAp{\cstSizef}{T} \ge n$.
  \textbf{(3)}~Furthermore, we prove that for every Boolean list $\epsilon = [\epsilon_1,\ldots,\epsilon_n]$ there exists a $T \in \isaAp{\cst{set}}{Ts}$ that contains the corresponding variables $x_{1,\epsilon_1},\ldots,x_{n,\epsilon_n}$ positively.
  If this is not the case for a specific list $\epsilon$, we show using step 1 that every $T$ must contain one of the variables $x_{1,\neg\epsilon_1},\ldots,x_{n,\neg\epsilon_n}$ positively.
  We then define a valuation that maps all variables $x_{i,\epsilon_i}$ to true and $x_{i,\neg\epsilon_i}$ to false for $i \in \{1,\ldots,n\}$.
  We show using \cref{aux_lemma_prop4_step3} that this valuation satisfies $F_n$ but not $G$.
  This means that $F_n$ is not equivalent to $G$, which contradicts the \lcnamecref{thm_cnf_to_dnf_blowup}'s assumption.
  \textbf{(4)}~We then prove that the number of Boolean lists of length $n$ is $2^n \le \setCard{\isaAp{\cst{set}}{Ts}} \le \listLength{Ts}$ using steps 1 and 3.
  \textbf{(5)}~Finally, we show that $\isaAp{\cstSizef}{G} \ge n \cdot 2^n$ by combining the results from steps 2 and 4 to discharge the assumptions of \cref{thm_exp_size}.
\end{proof}

\Cref{thm_cnf_to_dnf_blowup} assumes the existence of two equivalent formulas $F_n$ (in conjunctive normal form) and $\bigvee_{i = 0}^{\listLength{Ts}} \listNth{Ts}{i}$ (in disjunctive normal form) and proves a lower bound on the size of the later.
We now prove that such equivalent formulas do exist and that the exponential blowup holds for all of them.

\begin{theorem}[Exponential Blowup from CNF to DNF]
  \label{thm_cnf_to_dnf_blowup_math}
  Let $n$ be a natural number.
  There exists a formula $\phi$ in conjunctive normal form with size $\left|\phi\right| = 10 \cdot n + 2$ such that every equivalent formula $\psi$ in disjunctive normal form has size $\left|\psi\right| \ge n \cdot 2^n$.
\end{theorem}
\begin{proof}
  We start by a case analysis on $n$.
  The interesting case is when $n > 0$.
  We define $\phi = F_n$ (see \cref{def_Fn});
  it is easy to show that $F_n$ is in conjunctive normal form (using \cref{thm_is_cnf_Fn}) and that $\left|\phi\right| = 10 \cdot n + 2$ (using \cref{thm_sizef_Fn}).
  To prove the last property, we assume that there is an equivalent formula $\psi$ in disjunctive normal form.
  We first obtain a list $Ts$ (by filtering $\isaAp{\cst{undisj}}{\psi}$) of all satisfiable terms in $\psi$ such that
  all formulas in $Ts$ are conjunctive and satisfiable,
  %TODO: think whether we could explain the trick with filter and undisj. Maybe extract to its own lemma?
  $\isaAp{\cstSizef}{\psi} \ge \isaAp{\cstSizef}{(\bigvee_{i = 0}^{\listLength{Ts}} \listNth{Ts}{i})}$ (using \cref{thm_sizef_BigOr_undnf_BigAnd_unconj}),
  and $\bigvee_{i = 0}^{\listLength{Ts}} \listNth{Ts}{i}$ is equivalent to $\psi$.
  From this last equivalence, we show that $\bigvee_{i = 0}^{\listLength{Ts}} \listNth{Ts}{i}$ is equivalent to $F_n$.
  This allows us to prove that $\isaAp{\cstSizef}{(\bigvee_{i = 0}^{\listLength{Ts}} \listNth{Ts}{i})} \ge n \cdot 2^n$ using \cref{thm_cnf_to_dnf_blowup}.
  We can prove that $\isaAp{\cstSizef}{\psi} \ge n \cdot 2^n$ by transitivity and, finally, that $\left|\psi\right| \ge n \cdot 2^n$ using \cref{thm_sizef_le_size}.
\end{proof}

We claim that \cref{thm_cnf_to_dnf_blowup_math} is a formalization of the paper proof of \cref{dnfblowup}.


\section{DNF to CNF}
\label{sec_DNF_to_CNF}

Our formalization splits the paper proof of \cref{cnfblowup} again in a low-level \lcnamecref{thm_dnf_to_cnf_blowup}, which assumes the existence of formulas with specific shapes and properties, and a \lcnamecref{thm_dnf_to_cnf_blowup_math}, which proves that such formulas can always be obtained.
We start by defining a function to produce the dual of a formula and proving a few basic properties.

\begin{definitionx}
  \label{def_dualize}
  The function $\tyAnnot{\cst{dualize}}{\tyFormula{\yA} \Rightarrow \tyFormula{\yA}}$ converts a formula to its dual form by replacing each literal with its complementary:
  an atom $A$ becomes $\neg A$,
  falsehood $\bot$ becomes $\neg\bot$,
  a negation $\neg \phi$ becomes $\phi$,
  a disjunction $\phi \lor \psi$ becomes $\isaAp{\cst{dualize}}{\phi} \land \isaAp{\cst{dualize}}{\psi}$,
  a conjunction $\phi \land \psi$ becomes $\isaAp{\cst{dualize}}{\phi} \lor \isaAp{\cst{dualize}}{\psi}$, and
  an implication $\phi \rightarrow \psi$ becomes $\phi \land \isaAp{\cst{dualize}}{\psi}$.
\end{definitionx}

\begin{remark*}
  In contrast to the definition of dual formulas in \cref{sec_background}, we specify the $\cst{dualize}$ function for arbitrary formulas to avoid the recurring assumption that formulas are in negation normal form.
\end{remark*}

The semantics of the dual of a formula is the exact opposite of that of the original formula.

\begin{lemma}
  \label{thm_equiv_dualize}
  Let $\phi$ be a formula.
  The formula $\isaAp{\cst{dualize}}{\phi}$ is equivalent to $\neg\phi$.
\end{lemma}

Because the $\cstSizef$ function ignores negations, the $\cst{dualize}$ function preserves the size of the original formula.

\begin{lemma}
	\label{thm_sizef_dualize}
  Let $\phi$ be a formula.
  $\isaAp{\cstSizef}{(\isaAp{\cst{dualize}}{\phi})} = \isaAp{\cstSizef}{\phi}$.
\end{lemma}

Next, we prove some sufficient condition for a dual formula to be satisfiable.

\begin{lemma}
  \label{thm_dualized_disj_not_taut_impl_sat}
	Let $\phi$ be a formula.
	If $\phi$ is disjunctive and not tautological,
	then $\isaAp{\cst{dualize}}{\phi}$ is satisfiable.
\end{lemma}

Next, we prove that the dual of some specific formulas in disjunctive normal form are in conjunctive normal form.

\begin{lemma}
  \label{thm_dualized_conj_of_disjs_is_disj_of_conjs}
  Let $Cs$ be a list of formulas.
  If all formulas in $Cs$ are disjunctive and not tautological,
  then there exists a list $Ts$ such that all formulas in $Ts$ are conjunctive and satisfiable, and $\isaAp{\cst{dualize}}{(\bigwedge_{i = 0}^{\listLength{Cs}} \listNth{Cs}{i})} = \bigvee_{i = 0}^{\listLength{Ts}} \listNth{Ts}{i}$.
\end{lemma}
\begin{proof}
  We prove this by structural induction over the list $Cs$ using \cref{thm_dualized_disj_not_taut_impl_sat}.
  The idea is to map each element of $Cs$ to its dual form.
\end{proof}

We can now prove that some specific formulas equivalent to the dual of $F_n$ (see \cref{def_Fn}) have a size exponential in $n$.

\begin{proposition}
  \label{thm_dnf_to_cnf_blowup}
  Let $n > 0$ be a natural number.
  Let $Cs$ be a list of formulas.
  Let $G = \bigwedge_{i = 0}^{\listLength{Cs}} \listNth{Cs}{i}$.
  If all formulas in $Cs$ are disjunctive and not tautological, and $\isaAp{\cst{dualize}}{F_n}$ is equivalent to $G$,
  then $\isaAp{\cstSizef}{G} \ge n \cdot 2^n$.
\end{proposition}
\begin{proof}
  We prove this by contradiction using \cref{thm_cnf_to_dnf_blowup}.
  We assume $\isaAp{\cstSizef}{G} < n \cdot 2^n$ and from that derive false in five steps.
  \textbf{(1)}~First, we know from \cref{thm_sizef_dualize} that $\isaAp{\cstSizef}{(\isaAp{\cst{dualize}}{G})} = \isaAp{\cstSizef}{G}$, thus $\isaAp{\cstSizef}{(\isaAp{\cst{dualize}}{G})} < n \cdot 2^n$.
  \textbf{(2)}~Moreover, we show that $\isaAp{\cst{dualize}}{G}$ is equivalent to $F_n$ using, among others, \cref{thm_equiv_dualize}.
  \textbf{(3)}~Also, we prove using \cref{thm_dualized_conj_of_disjs_is_disj_of_conjs} that there exists a list $Ts$, where every formula contained in $Ts$ is both conjunctive and satisfiable, such that $\isaAp{\cst{dualize}}{G}$ is syntactically equal to $\bigvee_{i = 0}^{\listLength{Ts}} \listNth{Ts}{i}$.
  \textbf{(4)}~From steps 2 and 3, it follows that \cref{thm_cnf_to_dnf_blowup} holds for $F_n$ and $\isaAp{\cst{dualize}}{G}$.
  According to \cref{thm_cnf_to_dnf_blowup}, it therefore must be the case that $\isaAp{\cstSizef}{(\isaAp{\cst{dualize}}{G})} \ge n \cdot 2^n$.
  \textbf{(5)}~The result of step 1 contradicts the result of step 4, completing the proof.
\end{proof}

\Cref{thm_dnf_to_cnf_blowup} assumes the existence of two equivalent formulas $\isaAp{\cst{dualize}}{F_n}$ (in disjunctive normal form) and $\bigwedge_{i = 0}^{\listLength{Cs}} \listNth{Cs}{i}$ (in conjunctive normal form) and proves a lower bound on the size of the later.
We now prove that such equivalent formulas do exist and that the exponential blowup holds for all of them.

\begin{theorem}[Exponential Blowup from DNF to CNF]
  \label{thm_dnf_to_cnf_blowup_math}
  Let $n$ be a natural number.
  There exists a formula $\phi$ in disjunctive normal form with size $\left|\phi\right| = 10 \cdot n + 1$ such that every equivalent formula $\psi$ in conjunctive normal form has size $\left|\psi\right| \ge n \cdot 2^n$.
\end{theorem}
\begin{proof}
  The proof is analogous to the proof of \cref{thm_cnf_to_dnf_blowup_math}.
\end{proof}

We claim that \cref{thm_dnf_to_cnf_blowup_math} is a formalization of the paper proof of \cref{cnfblowup}.

\section{Discussion}
\label{sec_discussion}

Whereas most ideas of the paper proof can be directly used for the formalization, some intuitive mathematical proof steps required significant formalization effort.
This was most pronounced in the proof steps 2 and 4 of \cref{thm_cnf_to_dnf_blowup}, where we derived statements about the size of a formula from its properties.
To prove step 4, we use a general lemma (already included in Isabelle/HOL) about the cardinality of finite sets.

\begin{lemma}
  \label{card_inj_on_le}
  Let $X$ and $Y$ be sets. Let $f$ be a unary function.
  If $Y$ is finite, and
  if $\setImage{x}{f}{X} \subseteq Y$, and
  if $f$ is injective on $X$,
  then the cardinality of $X$ is less than or equal to the cardinality of $Y$.
\end{lemma}

We use \cref{card_inj_on_le} to prove that the number of Boolean lists of length $n$ is less than or equal to the cardinality of $\isaAp{\cst{set}}{Ts}$.
We instantiate $X$ with the set of all Boolean lists of length $n$,
$Y$ with $\isaAp{\cst{set}}{Ts}$, and
$f$ with a function that takes a Boolean list and returns some conjunctive formula $T \in \isaAp{\cst{set}}{Ts}$ for which the predicate of step 3 holds.
Finding an appropriate function for $f$ and discharging \cref{card_inj_on_le}'s assumptions was surprisingly challenging:
The first assumption was the most intricate and its proof uses both steps 1 and 3,
the second assumption follows from step 3,
the third assumption easily holds.

To prove step 2, we introduced a new lemma, inspired by \cref{card_inj_on_le}, that uses a binary function that is right total w.r.t.\ the target set.

\begin{lemma}
	\label{card_le_card_if_mem_imp_ex_mem}
	Let $X$ be a set of elements of type $\yA$ and $Y$ be a set of elements of type $\yC$.
	Let $\tyAnnot{f}{\yA \Rightarrow \yB \Rightarrow \yC}$ be a binary function.
	If $X$ and $Y$ are finite sets, and
	if for all $x \in X$ there exists a $y$ such that $\isaApAp{f}{x}{y} \in Y$, and
	if the function $f$ is injective on all those pairs of $x$ and $y$,
	then the cardinality of $X$ is less than or equal to the cardinality of $Y$.
\end{lemma}

We use \cref{card_le_card_if_mem_imp_ex_mem} to prove that every $T \in \isaAp{\cst{set}}{Ts}$ contains at least $n$ atoms and thus that $\isaAp{\cstSizef}{T} \ge n$.
To do so, we instantiate $X$ with $\{1,\ldots,n\}$, $Y$ with the set of atoms of $T$, and the $f$ with the constructor for variables.
Finding the correct statement for \cref{card_le_card_if_mem_imp_ex_mem} was challenging but, once found, it was easy to discharge the assumptions using step 1.\\

Moreover, we chose to modify the functions \textsf{BigAnd} and \textsf{BigOr} from Michaelis and Nipkow \cite{Propositional_Proof_Systems-AFP} and to define our own versions in \cref{def_BigOr_BigAnd}, where the base cases are handled differently.
Our functions avoid appending $\lnot\bot$ and $\bot$ to the produced formula when the provided list is nonempty.
This allowed us to prove the size equalities in \cref{thm_sizef_BigOr_undnf_BigAnd_unconj} instead of having the left-hand side of the lemmas be slightly smaller than the right-hand side.
Using the original functions would have been possible, but it would have resulted in \cref{thm_cnf_to_dnf_blowup_math,thm_dnf_to_cnf_blowup_math} stating that the size $\left|\psi\right| + 2 \ge n \cdot 2^n$ instead of $\left|\psi\right| \ge n \cdot 2^n$.
To keep the statements as close as possible to their mathematical counterparts, we therefore chose to introduce the functions \textsf{BigAnd'} and \textsf{BigOr'}.

Finally we introduced the function $\cstSizef$ to be able to prove the equality of \cref{thm_sizef_dualize}.
Without $\cstSizef$, we could only prove the inequality $\left|\isaAp{\cst{dualize}}{\phi}\right| \le 2 \cdot \left|\phi\right|$ because $\cst{dualize}$ adds and removes negations depending on the formula.
It would be possible to restate and prove the statement of \cref{thm_cnf_to_dnf_blowup} in terms of $\left|\cdot\right|$.
However, if we tried to restate and prove the statement of \cref{thm_dnf_to_cnf_blowup} in terms of $\left|\cdot\right|$, we would prove $\left|\isaAp{\cst{dualize}}{G}\right| < 2 \cdot n \cdot 2^n$ in step 1 and $\left|\isaAp{\cst{dualize}}{G}\right| \ge n \cdot 2^n$ in step 4, and we would not be able to derive a contradiction in step~5.


\section{Conclusion}
\label{sec_concl}

We formalized in Isabelle/HOL the well-known folklore result about propositional logic that transforming a formula in disjunctive or conjunctive normal form can lead to an exponential blowup of the formula size.
The proof is split into a low-level \lcnamecref{thm_cnf_to_dnf_blowup} and a \lcnamecref{thm_cnf_to_dnf_blowup_math} for conversion to disjunctive normal form and analogously for conversion to conjunctive normal form.
We also reported of some challenges we faced we encountered when proving statements about the size of formula.

%%
%% Bibliography
%%

%% Please use bibtex,

\bibliography{bib.bib}

\end{document}
